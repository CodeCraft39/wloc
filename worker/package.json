{
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "check": "wrangler deploy --dry-run --outdir .wrangler/dry-run"
  },
  "dependencies": {
    "hono": "4.12.29"
  },
  "devDependencies": {
    "wrangler": "4.110.0"
  }
}
